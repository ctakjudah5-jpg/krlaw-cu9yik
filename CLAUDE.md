# 실습 규칙 (모든 작업에서 반드시 지킬 것)

## 만드는 것
- 내가 시킨 파일만 C:\krlaw 안에 만든다. 이 실습에서 만드는 것은 다음뿐이다.
    my_law_server.py   MCP 서버
    setup_key.py       인증키 입력창 (저장소에서 받아 그대로 저장)
    07_register.py     등록 프로그램 (저장소에서 받아 그대로 저장)
    등록하기.bat        위 등록 프로그램을 더블클릭으로 실행하는 파일
  (이 규칙 파일과, 실행 중 생기는 law_oc.txt·owner.txt 는 예외)
- 작업폴더 C:\krlaw 밖의 파일은 손대지 않는다.
- ★Claude 앱의 설정 파일(claude_desktop_config.json)은 절대 직접 고치지 않는다.
  앱이 켜져 있으면 앱이 자기 설정을 다시 써서 고친 내용을 지워 버리기 때문이다.
  등록은 사용자가 앱을 완전히 끈 뒤 등록하기.bat 을 직접 실행해서 한다.
- ★Claude 앱의 어떤 파일도 열람하거나 내용을 출력하지 마라
  (claude_desktop_config.json, config.json 등 — 로그인 토큰이 들어 있다).
  등록 상태가 의심되면 파일을 여는 대신 "등록하기.bat 을 다시 실행하세요"라고만 안내한다.
- 등록하기.bat 과 07_register.py 를 네가 실행하지 마라. 실행은 사용자가 한다.
- Claude 앱 종료가 필요하면 사용자가 작업 관리자로 직접 한다.
  taskkill 같은 명령으로 죽이려 하지 마라(Store 앱은 명령으로 안 죽는다 — 시간만 버린다).
- 인증키를 셸 명령줄에 넣지 마라(예: curl 의 OC= 인자에 파일 내용 대입 금지).
  키가 필요한 호출은 전부 파이썬 코드 안에서 read_key() 로만 처리한다.

## 절대 규칙
- 파이썬 표준 라이브러리만 쓴다(json, os, sys, re, urllib, time, html, tkinter, xml).
  pip install · 가상환경 · 외부 패키지 · mcp · fastmcp 전부 금지.
- 법제처 인증키 값을 화면 · 로그 · URL · 네 답변 어디에도 출력하지 않는다.
  길이나 앞 몇 글자도 말하지 않는다. 확인이 필요하면 "확인됨" / "확인 안 됨" 으로만 말한다.
- stdout 은 JSON-RPC 전용 채널이다. print() 로 로그를 찍지 말고 전부 stderr 로 보낸다.
- 시작할 때 stdin/stdout/stderr 를 UTF-8 로 맞추고 stdout 은 newline="\n" 으로 고정한다.
  (한글 Windows 에서 이걸 빠뜨리면 연결이 안 된다)

## GitHub 참고자료를 볼 때
- 내가 지정한 파일만 읽는다. 저장소의 다른 파일을 스스로 열지 않는다.
- 주소는 내가 준 것을 그대로 쓴다.
- 참고 파일은 개념별 예제다. 통째로 이어붙이지 말고, 원리를 이해한 뒤
  내 서버 파일(my_law_server.py) 하나에 맞게 옮겨 쓴다.

## 작업 방식
- 한 번에 한 단계만 한다. 지시에 없는 기능을 미리 만들지 않는다.
- 파일을 고치기 전에 무엇을 할지 3줄로 먼저 보여준다. 보여준 뒤에는 승인을 기다리지 말고
  바로 진행한다. (강의 중이라 매번 멈추면 진도가 나가지 않는다)
  단, 지시에 없는 파일을 지우거나 덮어써야 할 것 같으면 그때는 반드시 먼저 물어본다.
- 만든 뒤에는 네가 직접 실행해서 결과를 보여준다. 된다고 말만 하지 않는다.
